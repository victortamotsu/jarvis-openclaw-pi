version: "3.8"

services:
  firefly-db:
    image: mariadb:latest
    container_name: firefly-db-new
    restart: unless-stopped
    mem_limit: "512m"
    environment:
      MYSQL_ROOT_PASSWORD: ${FIREFLY_DB_PASSWORD:-firefly}
      MYSQL_DATABASE: firefly
      MYSQL_USER: firefly
      MYSQL_PASSWORD: ${FIREFLY_DB_PASSWORD:-firefly}
    volumes:
      - firefly_iii_firefly_db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 60s
      timeout: 10s
      retries: 3
    networks:
      - jarvis

  firefly-iii:
    image: fireflyiii/core:latest
    container_name: firefly-iii-new
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      DB_CONNECTION: mysql
      DB_HOST: firefly-db
      DB_PORT: 3306
      DB_DATABASE: firefly
      DB_USERNAME: firefly
      DB_PASSWORD: ${FIREFLY_DB_PASSWORD:-firefly}
      TRUSTED_PROXIES: "**"
      APP_KEY: ${APP_KEY:-base64:somerandomkeyherefordev}
    volumes:
      - firefly_iii_firefly_app_data:/var/www/html/storage
    depends_on:
      - firefly-db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 120s
    networks:
      - jarvis

  openclaw:
    image: python:3.11-slim
    container_name: openclaw-gateway
    restart: unless-stopped
    mem_limit: "1g"
    command: python -c "import time; [print(f'OpenClaw Agent waiting... {i}', flush=True) or time.sleep(60) for i in range(60*24)]"
    environment:
      - OPENCLAW_MODEL=copilot
      - OPENCLAW_AGENT_ID=jarvis
      - OPENCLAW_LOG_LEVEL=info
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GOOGLE_REFRESH_TOKEN=${GOOGLE_REFRESH_TOKEN}
      - FIREFLY_URL=http://firefly-iii:8080
      - FIREFLY_TOKEN=${FIREFLY_TOKEN}
    volumes:
      - ./config/openclaw:/config/openclaw
      - ${EXTERNAL_MOUNT:-/mnt/external}/openclaw:/data/openclaw
      - ${EXTERNAL_MOUNT:-/mnt/external}/logs/openclaw:/var/log/openclaw
    ports:
      - "127.0.0.1:3000:3000"  # Loopback only
    depends_on:
      firefly-iii:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - jarvis

  mcporter:
    image: alpine:3.19
    container_name: mcporter-firefly
    restart: unless-stopped
    mem_limit: "128m"
    environment:
      - MCPORTER_DEBUG=false
    command: echo "mcporter service running (placeholder)"
    depends_on:
      - firefly-iii
    healthcheck:
      test: ["CMD", "test", "-f", "/etc/os-release"]
      interval: 60s
      timeout: 10s
      retries: 3
    networks:
      - jarvis

  scheduler:
    image: alpine:3.19
    container_name: jarvis-scheduler
    restart: unless-stopped
    mem_limit: "256m"
    environment:
      # Passed so scripts inside cron jobs can authenticate without local installs
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - FIREFLY_TOKEN=${FIREFLY_TOKEN}
      - EXTERNAL_MOUNT=${EXTERNAL_MOUNT:-/mnt/external}
    volumes:
      - ./config/crontabs:/etc/crontabs
      - ./scripts:/app/scripts
      - ./pipeline:/app/pipeline
      - ./config:/app/config
      - ${EXTERNAL_MOUNT:-/mnt/external}:/mnt/external
      - /var/run/docker.sock:/var/run/docker.sock
    command: >
      sh -c "
        apk add --no-cache
          dcron bash curl git git-crypt
          sqlite jq logrotate
          github-cli
          python3 py3-pip &&
        pip3 install --no-cache-dir --quiet requests &&
        cp /etc/crontabs/jarvis /var/spool/cron/crontabs/root &&
        crond -f -l 2
      "
    depends_on:
      - openclaw
    healthcheck:
      test: ["CMD", "test", "-f", "/var/spool/cron/crontabs/root"]
      interval: 60s
      timeout: 10s
      retries: 3
    networks:
      - jarvis

volumes:
  firefly_iii_firefly_db_data:
    external: true
  firefly_iii_firefly_app_data:
    external: true

networks:
  jarvis:
    driver: bridge
